{
  "name": "anewone-site",
  "private": true,
  "type": "module",
  "description": "A NEW ONE — static floor (docs/) plus the Vercel functions under api/. The chat function keeps memory on Walrus through MemWal.",
  "engines": { "node": "22.x" },
  "dependencies": {
    "@mysten-incubation/memwal": "^0.1.8",
    "ethers": "^6.13.0"
  }
}
